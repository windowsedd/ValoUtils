electron-builder.env

# Claude Code memory and temp
.claude/
.tmp/

# IDE / build artifacts
*.iml
tsconfig.node.tsbuildinfo

# Exported replay JSON files (runtime artifacts)
replay-*.json

# Built parser binary (generated by npm run build:parser)
resources/bin/ValorantReplayParser.exe
resource/ValorantReplayParser.exe

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release
*.local

# Tauri build output + updater signing key (never commit the private key)
src-tauri/target
src-tauri/gen
src-tauri/*.key
**/cert/*.key
**/*.pfx

# Compiled sidecar binary (rebuilt via `bun build --compile`, see sidecar/)
src-tauri/binaries/*.exe

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
